# Staging Models

This lane contains staging warehouse model artifacts for SynapseIQ.

Purpose:
- normalize raw records into event, entity, link, mapping, finding, and activation families
- preserve confidence and provenance
- support downstream canonical consolidation

These models should follow `docs/specs/warehouse-contract.md`.
