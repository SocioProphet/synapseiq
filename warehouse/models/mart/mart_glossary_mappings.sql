-- Glossary mappings mart stub
-- Purpose: expose consumer-friendly tabular to glossary mapping results.

select
  cast(null as string) as record_id,
  cast(null as string) as table_name,
  cast(null as string) as column_name,
  cast(null as string) as mapping_target,
  cast(null as float64) as overall_confidence,
  cast(null as string) as explanation_ref
where 1 = 0;
