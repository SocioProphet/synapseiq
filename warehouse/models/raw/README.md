# Raw Models

This lane contains raw warehouse model artifacts for SynapseIQ.

Purpose:
- preserve append-only landed records
- preserve canonical envelopes and permitted source-native payloads
- support replay and audit

These models should follow `docs/specs/warehouse-contract.md`.
