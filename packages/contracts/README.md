# Contracts Package

This package will hold typed interfaces and runtime contract definitions for:
- envelopes
- adapters
- stream payloads
- warehouse record families
- findings and activations

The code in this package should mirror the normative docs in `docs/specs/`.