# Ontology Package

This package will contain the semantic core for SynapseIQ.

Expected contents:
- UDM-aligned canonical ontology assets
- gist alignment notes where useful
- domain ontology bridges such as FIBO, UCO, SCO, and related vocabularies
- glossary mapping metadata
- ontology version metadata
- helper utilities for semantic lookup and alignment

This package is where semantic meaning becomes explicit.