# Enrichment Package

This package will contain vendor-specific adapters and reusable enrichment operators.

Expected lanes:
- vendor adapters
- semantic ranking operators
- glossary mapping enrichers
- ontology alignment enrichers
- explanation generation helpers

Adapters should consume the shared contracts package and emit canonical envelopes.