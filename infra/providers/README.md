# Provider Overlays

This workspace contains provider-specific infrastructure implementations for SynapseIQ.

The product architecture is capability-shaped first. Provider overlays are implementation mappings for those capabilities.

Examples:
- `gcp/`
- `aws/`
- `azure/`
- `kubernetes/`
- `local/`
