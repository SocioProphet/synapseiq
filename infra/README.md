# Infrastructure

This workspace contains deployment and runtime infrastructure definitions for SynapseIQ.

Expected lanes:
- `cloud-run/`
- `pubsub/`
- `bigquery/`
- `iam/`
- `monitoring/`

Infrastructure must enforce policy, retention, and least-privilege requirements defined in the specs.