# Services

This workspace contains runnable service surfaces for SynapseIQ.

Expected lanes:
- `enrichment-api/`
- `enrichment-collector/`
- `reasoning-api/`
- `control-plane/`

Services should consume the shared contract packages and remain thin where possible.